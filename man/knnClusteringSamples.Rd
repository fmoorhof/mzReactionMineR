% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knnClusteringSamples.R
\name{knnClusteringSamples}
\alias{knnClusteringSamples}
\title{knnClusteringSamples}
\usage{
knnClusteringSamples(
  object,
  assay,
  filter_type = "area",
  n_top = nrow(object),
  calc_PCA = TRUE,
  min_PC = 5,
  PC_var = 0.8,
  k = 5,
  resolution = 1,
  metric = "cosine",
  scale = TRUE,
  id_col = "id"
)
}
\arguments{
\item{object}{a SummarizedExperiment object}

\item{assay}{Character. The name of the assay to be used for clustering.}

\item{calc_PCA}{Logical. Wether to perform PCA before clustering.
Default is TRUE.}

\item{min_PC}{Integer. The minimum number of PCs to retain. Default is 5.}

\item{PC_var}{Numeric. The minimum cumulative variance that the retained PCs should explain.}

\item{k}{Integer. The number of neighbors to use for the k-nearest neighbor graph. Default is 5.}

\item{resolution}{Numeric. Resolution parameter for the Louvain algorithm. Default is 1.}

\item{metric}{Either "euclidean", "manhattan" or "cosine". The distance
metric to use for calculating the distance matrix. Default is "cosine".}

\item{scale}{Logical. Whether to scale the data before calculating the
distance matrix. Default is TRUE.}

\item{id_col}{Character. The name of the column in rowData(object) that
contains the feature ids. Default is "id".}

\item{n_topVar}{Integer. The number of features to select based on rowwise
variance for clustering. Defaults to all features.}
}
\value{
a data.frame with two columns: "sample" and "cluster".
}
\description{
A function that performs clustering (community detection) on samples in a
SummarizedExperiment object based on a k-nearest neighbor graph.
}
